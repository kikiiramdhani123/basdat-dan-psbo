<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kiki.myheroapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MenuSoal"/>
        <activity android:name=".MenuModul"/>
        <activity android:name=".MenuGuru"/>
        <activity android:name=".MenuKategori"/>
        <activity android:name=".MenuSoalDummy"/>
        <activity android:name=".MenuGuruDummy"/>
        <activity android:name=".MenuBuatSoal"/>
    </application>

</manifest>